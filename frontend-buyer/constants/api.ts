const BASE = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3001/api';

export const API_ENDPOINTS = {
  // ── Auth ─────────────────────────────────────────────────────────────────
  AUTH: {
    REGISTER: `${BASE}/auth/register`,
    LOGIN: `${BASE}/auth/login`,
    GOOGLE: `${BASE}/auth/google`,
    FACEBOOK: `${BASE}/auth/facebook`,
  },

  // ── Users ─────────────────────────────────────────────────────────────────
  USERS: {
    ME: `${BASE}/users/me`,
    ADDRESSES: `${BASE}/users/me/addresses`,
    ADDRESS: (id: string) => `${BASE}/users/me/addresses/${id}`,
  },

  // ── Products ──────────────────────────────────────────────────────────────
  PRODUCTS: {
    LIST: `${BASE}/products`,
    DETAIL: (slug: string) => `${BASE}/products/${slug}`,
  },

  // ── Shops ─────────────────────────────────────────────────────────────────
  SHOPS: {
    DETAIL: (slug: string) => `${BASE}/shops/${slug}`,
    APPLY: `${BASE}/shops/apply`,
  },

  // ── Cart & Orders ─────────────────────────────────────────────────────────
  ORDERS: {
    CART: `${BASE}/orders/cart`,
    CART_ITEM: (productId: string) => `${BASE}/orders/cart/${productId}`,
    CHECKOUT: `${BASE}/orders/checkout`,
    MINE: `${BASE}/orders/mine`,
  },
} as const;
